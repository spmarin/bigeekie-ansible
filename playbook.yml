- name: Playbook Ejemplo
  hosts: test
  gather_facts: true
  connection: local
  tags: always
  tasks:
    - shell: more /etc/hosts

- name: Playbook Remoto Ejemplo
  hosts: remote
  gather_facts: true
  tags: always
  tasks:
    - shell: more /etc/hosts

- name: Playbook Hostvars
  hosts: remote
  tags: always
  tasks:
    - debug:
        msg: "{{ hostvars[item].ansible_default_ipv4.address }}"
      with_items: "{{ groups.all }}"
